# Database Configuration
DATABASE_URL="postgresql://user:password@localhost:5432/dbname?sslmode=require"
PGDATABASE="dbname"
PGHOST="localhost"
PGPORT="5432"
PGUSER="user"
PGPASSWORD="password"

# Session Configuration
SESSION_SECRET="your-secret-here"

# Firebase Configuration
FIREBASE_CLIENT_EMAIL="your-client-email@project.iam.gserviceaccount.com"
FIREBASE_PRIVATE_KEY="your-private-key"
GOOGLE_API_KEY="your-api-key"

# Frontend Firebase Configuration
VITE_FIREBASE_API_KEY="your-api-key"
VITE_FIREBASE_APP_ID="your-app-id"
VITE_FIREBASE_PROJECT_ID="your-project-id"

# Cache Configuration (Optional)
# Set CACHE_ENABLED=false to run without caching
CACHE_ENABLED=false
CACHE_TTL=900
CACHE_MIN_RECORDS=1000

# Type-specific cache settings (Only needed if CACHE_ENABLED=true)
CACHE_CANDIDATES_ENABLED=false
CACHE_CANDIDATES_TTL=900
CACHE_CANDIDATES_MIN_RECORDS=1000

CACHE_EMPLOYERS_ENABLED=false
CACHE_EMPLOYERS_TTL=900
CACHE_EMPLOYERS_MIN_RECORDS=500

CACHE_JOBS_ENABLED=false
CACHE_JOBS_TTL=900
CACHE_JOBS_MIN_RECORDS=1000

# Redis Configuration (Only needed if CACHE_ENABLED=true)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_password
